sudo apt purge nginx nginx-common nginx-core

sudo rm /etc/nginx/sites-enabled/npm-proj
sudo vim /etc/nginx/sites-enabled/nginx.conf

cp -r ./build/. /usr/share/nginx/html
sudo cp -r build /usr/share/nginx/html
-------------------------------------------------------------------------------------------------------------------------------------------------------
1.Check existing file: 
  ls -l /etc/nginx/sites-enabled/nginx.conf
  
2.Remove the default Nginx welcome page to avoid conflicts.
  sudo unlink /etc/nginx/sites-enabled/default
  
3.Re-run your link command: 
  sudo ln -s /etc/nginx/sites-available/nginx.conf /etc/nginx/sites-enabled/

Final Step: Restart Nginx
  Test configuration: 
  sudo nginx -t
  Restart service: 
  sudo systemctl restart nginx 
  (or sudo service nginx restart)
  
*****************************************************************************************************************************************************
Option 2: 
1.Remove the existing file manually
2.Remove it: 
sudo rm /etc/nginx/sites-enabled/nginx.conf

  sudo rm /etc/nginx/sites-available/npm-proj-build
  sudo ln -s /etc/nginx/sites-available/npm-proj-build /etc/nginx/sites-enabled/

2.Enable the ConfigurationLink the file:Enable the site by creating a symbolic link in the sites-enabled directory.
   sudo ln -s /etc/nginx/sites-available/myproxy /etc/nginx/sites-enabled/ --> Disable the default
   
3.Remove the default Nginx welcome page to avoid conflicts.
  sudo unlink /etc/nginx/sites-enabled/default
  
Test and Reload: 
Check for syntax errors and apply the changes.
sudo nginx -t
sudo systemctl restart nginx
**********************************************************************************************************************************
Option 1: Force the symlink (Easiest)
sudo ln -sf /etc/nginx/sites-available/nginx.conf /etc/nginx/sites-enabled/


/etc/nginx/sites-available/npm-proj 
/usr/share/nginx/html
root /var/www/html